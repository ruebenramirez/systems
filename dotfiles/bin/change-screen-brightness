#!/usr/bin/env bash

change="${1:?Usage: $0 <degrees>}"

if (( change > 0 )); then
    brightnessctl set "+${change}%"
else
    brightnessctl set "${change}%"
fi

current=$(brightnessctl -m | cut -d, -f4 | tr -d '%')

if (( current < 1 )); then
    brightnessctl set 1%
fi

